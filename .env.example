# Optional legacy/CI alternative. The recommended local workflow is:
#   lit-harvest auth set elsevier university_primary
#
# Secrets set here are read only when a credential uses env:NAME.
# ELSEVIER_API_KEY_1=replace-with-your-own-key
